import pandas as pd\nfrom sklearn.model_selection import train_test_split, GridSearchCV\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.ensemble import RandomForestRegressor\nfrom sklearn.metrics import mean_squared_error, r2_score\n\n# Load dataset\ndf = pd.read_csv('data/sales_data.csv')  # Update this line with the correct path to your dataset\n\n# Preprocess data (example)\n# df = preprocess_data(df)  # You might need to implement a preprocessing function depending on your dataset structure\n\n# Features and target variable\nX = df.drop('target_column', axis=1)  # Replace 'target_column' with your actual target name\nY = df['target_column']\n\n# Split the dataset\nX_train, X_test, Y_train, Y_test = train_test_split(X, Y, test_size=0.2, random_state=42)\n\n# Training Linear Regression Model\nlinear_reg = LinearRegression()\nlinear_reg.fit(X_train, Y_train)\nY_pred_linear = linear_reg.predict(X_test)\n\n# Evaluate Linear Regression Model\nprint('Linear Regression Model')\nprint('Mean Squared Error:', mean_squared_error(Y_test, Y_pred_linear))\nprint('R^2 Score:', r2_score(Y_test, Y_pred_linear))\n\n# Hyperparameter tuning for Random Forest\nparam_grid = {\n    'n_estimators': [50, 100, 200],\n    'max_features': ['auto', 'sqrt'],\n    'max_depth': [None, 10, 20, 30],\n    'min_samples_split': [2, 5, 10]\n}  \n\nrandom_forest = RandomForestRegressor(random_state=42)\ngrid_search = GridSearchCV(estimator=random_forest, param_grid=param_grid, cv=3, scoring='neg_mean_squared_error', n_jobs=-1)\ngrid_search.fit(X_train, Y_train)\n\n# Best Random Forest Model\nbest_random_forest = grid_search.best_estimator_\nY_pred_rf = bbest_random_forest.predict(X_test)\n\n# Evaluate Random Forest Model\nprint('\nRandom Forest Model')\nprint('Best Parameters:', grid_search.best_params_)\nprint('Mean Squared Error:', mean_squared_error(Y_test, Y_pred_rf))\nprint('R^2 Score:', r2_score(Y_test, Y_pred_rf))\n